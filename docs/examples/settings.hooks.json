{
  "_comment": [
    "Example PreToolUse hook that blocks edits to pom.xml.",
    "To enable it:",
    "  1. Copy this file to .claude/settings.json (merge if you already have one).",
    "  2. Copy docs/examples/hooks/pom-guard.sh to .claude/hooks/pom-guard.sh",
    "     and make it executable: chmod +x .claude/hooks/pom-guard.sh",
    "  3. Start a new Claude Code session so the hook is picked up.",
    "The hook reads the tool input as JSON on stdin. Exit code 2 blocks the tool call",
    "and feeds stderr back to Claude as the reason."
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/pom-guard.sh"
          }
        ]
      }
    ]
  }
}
