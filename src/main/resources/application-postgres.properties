# PostgreSQL 16, as started by `docker compose up -d db`.
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/ops}
spring.datasource.username=${DATABASE_USER:ops}
spring.datasource.password=${DATABASE_PASSWORD:ops}
